import * as S from './styles';

import GridCol from '@Components/GridCol';

export const Button = () => {
  return (
    <GridCol col={6} colLg={9}>
      <S.Button>Almost before we knew it, we had</S.Button>
    </GridCol>
  );
};

export default Button;
